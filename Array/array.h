#include <vector>
#include <string>
using namespace std;


class Solution {
public:
    /* 对角线遍历 */
    vector<int> findDiagonalOrder(vector<vector<int>>& matrix);

    /* 最长公共前缀 */
    string longestCommonPrefix(vector<string>& strs);
};